# Second-Order Differential Machine Learning

## Commands

We use hatch as the build system and environmnent manager:

`hatch run lint:fmt`

`hatch run test`

`hatch build`

### System wide install
`pip install -e .`
